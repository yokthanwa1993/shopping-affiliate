{
  "name": "clickreport-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy serving clickreport.wwoom.com -> the local Shopee zip runtime click-report contract.",
  "main": "src/index.js",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test \"test/**/*.test.js\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "engines": {
    "node": ">=18"
  }
}
