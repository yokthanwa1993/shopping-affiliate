{
  "name": "clickreport-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy serving clickreport.wwoom.com -> customlink.wwoom.com/click-report. Sibling app; does not modify affiliate-shortlink-cloak.",
  "main": "src/index.js",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test \"test/**/*.test.js\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "engines": {
    "node": ">=18"
  }
}
