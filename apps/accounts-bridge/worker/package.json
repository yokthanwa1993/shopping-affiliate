{
  "name": "accounts-bridge-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Accounts Bridge v2 — Cloudflare Worker + D1 backend for durable, ownership-explicit account/role/session/cookie/page-binding state",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test --experimental-sqlite test/*.test.js",
    "check": "node --check src/index.js && node --check src/router.js && node --check src/store.js && node --check src/lib.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply accounts-bridge-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply accounts-bridge-db --remote"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
