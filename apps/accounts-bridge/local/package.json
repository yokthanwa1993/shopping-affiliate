{
  "name": "accounts-bridge-local",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Accounts Bridge v2 — local profile-archive helper (download-before-open / upload-after-close, sealed). No secrets in repo: seal/open key material is injected by the operator app.",
  "main": "profile-archive.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check profile-archive.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
