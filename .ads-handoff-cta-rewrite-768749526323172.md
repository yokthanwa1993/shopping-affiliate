# aff-ads → aff-developer handoff: API for one-post CTA/comment campaign rewrite

Owner: aff-developer
Requester: Thanwa via aff-ads
Date: 2026-06-19
Repo: `/Users/yok-macmini/Developer/shopping-affiliate`

## Goal
Build a safe API for changing an existing Facebook Page/Reel post's Shopee tracking campaign (`sub1`) on both:
1. existing Page-owned comment link, and
2. existing CTA/button link.

The ads operator will later call this API when a post has strong orders on old campaign sub1 and needs to migrate to the current campaign.

## Canary item requested by Thanwa
- post_id / sub2 candidate from orders: `768749526323172`
- old shortlink in comment and CTA: `https://s.shopee.co.th/5q5l46qSw4`
- aff-ads pre-check expanded old shortlink and verified:
  - affiliate: `15130770000`
  - old `utm_content=1JUN26FBSPCAD-768749526323172-116759241338040--`
  - old sub1: `1JUN26FBSPCAD`
  - old sub2: `768749526323172`
  - old sub3/page candidate: `116759241338040`
  - product URL: `https://shopee.co.th/opaanlp/30233378/24790531216` (clean URL before affiliate params)
- IMPORTANT: do **not** hardcode เฉียบ `1008898512617594` for this canary. Resolve/confirm the real Page/post/story from the Facebook post and/or old link sub3. Expected page_id for the canary is likely `116759241338040` based on the old link expansion unless live Facebook resolve proves otherwise.
- expected Page story object should be `<resolved_page_id>_768749526323172` unless live resolve proves a different canonical Page story id; if different, report it clearly.
- current/target campaign sub1: `16JUN26FBSPCAD`
- old expanded campaign expected by user: `1JUN26FBSPCAD`
- affiliate/customlink id: `15130770000`
- comment text currently:
  ```text
  https://s.shopee.co.th/5q5l46qSw4
  📌 พิกัดอยู่ตรงนี้เลย กดเข้าไปดูเองได้
  🟠 สั่งผ่านลิงก์เพจเป็นพาร์ทเนอร์กับ Shopee ปลอดภัย 💯 ⏰
  ```
- CTA currently: `https://s.shopee.co.th/5q5l46qSw4`

## Required API behavior
Add/extend endpoint(s) so aff-ads can do this without code edits:

### 1) Read-only check endpoint
Suggested: `POST /api/dashboard/page-post-link-rewrite/check` or similar.
Input:
```json
{
  "page_id":"<optional; if omitted derive from old shortlink sub3 / Facebook resolve>",
  "post_id":"768749526323172",
  "url":"optional facebook/reel/share URL",
  "old_shortlink":"https://s.shopee.co.th/5q5l46qSw4"
}
```
Output must include:
- `ok`
- `page_id`
- `post_id`
- `page_story_object_id`
- `comment_id`
- `comment_from_id/name`
- `old_comment_message`
- `old_comment_shortlink`
- `old_comment_expanded_url`
- parsed old `affiliate_id`, `sub1`, `sub2`, `sub3`, `sub4` if present
- CTA: `cta_present`, `cta_type`, `old_cta_link`, `old_cta_expanded_url`, parsed old sub fields
- product URL cleaned from old shortlink expansion
- token/source metadata only in redacted/hash form if needed; never return tokens/secrets.

### 2) Preview endpoint
Suggested: `POST /api/dashboard/page-post-link-rewrite/preview`.
Input:
```json
{
  "page_id":"<optional; if omitted derive from old shortlink sub3 / Facebook resolve>",
  "post_id":"768749526323172",
  "target_sub1":"16JUN26FBSPCAD",
  "customlink_id":"15130770000",
  "surfaces":["comment","cta"],
  "dry_run":true,
  "allow_create_new":false
}
```
Output must include planned new shortlink generated through `customlink.wwoom.com`, target sub fields:
- `target_sub1=16JUN26FBSPCAD`
- `target_sub2=<canonical post_id tail>`
- `target_sub3=<resolved page_id, likely 116759241338040 for the canary based on old link expansion>`
- `target_sub4=<non-empty ledger/log id>` if this flow uses comment ledger. If you cannot provide sub4 safely, block real write and explain.
- `action=edit` for comment, not create.
- CTA action update, not ad/campaign changes.

### 3) Real canary run endpoint
Suggested: `POST /api/dashboard/page-post-link-rewrite/run`.
Must default safe:
- `dry_run` defaults true unless explicitly false.
- `batch_size=1` for this canary.
- `allow_create_new=false`.
- edit existing Page-owned comment only; do not create duplicate comment; do not delete.
- update CTA only for the same canonical post/story.
- verify after write.

Output/evidence required:
- same `comment_id` after edit
- old shortlink gone from comment
- new shortlink present in comment
- CTA final link is new shortlink
- expanded new link has `affiliate_id=15130770000` and `utm_content` with `16JUN26FBSPCAD-<post_id>-<resolved_page_id>-<sub4>-` (or if sub4 intentionally blank for CTA/comment flow, explain; preferred is non-empty sub4)
- `page_story_object_id` used; no bare Reel/video object fallback
- no secrets in output/logs

## Canary execution requested
After implementation/deploy/restart, run **one real canary only** for post `768749526323172` after a successful check + preview. Do not batch other posts.

## Verification evidence to return to aff-ads
Return concise Thai/English summary with:
- files changed
- test/check commands and results (`npm run check`, relevant tests)
- service/deploy/restart evidence
- check endpoint output summary for old comment+CTA link
- preview summary
- run/verify result for the single canary
- new shortlink
- expanded final URL parsed subs
- comment_id and story_id
- CTA final verification
- any blocker/error codes if blocked

## Hard rules
- Do not print/store tokens/cookies/secrets.
- Never delete/archive Facebook posts, comments, ads, adsets, campaigns.
- Default comment operation is edit-only, create_new=false.
- Page story object `<page_id>_<post_id>` only for comment read/write/verify; bare Reel/video IDs are metadata only.
- Stop on Graph block/rate/spam errors (368, 4, 17, 32, 613 etc.).
