# aff-ads → aff-developer handoff: deploy + one-post canary

Context: The one-post CTA/comment rewrite API was implemented in `apps/video-affiliate/worker`.

From previous dev summary:
- Changed:
  - `apps/video-affiliate/worker/src/index.ts`
  - `apps/video-affiliate/worker/src/comment-link-registry.ts`
  - `apps/video-affiliate/worker/test/page-post-link-rewrite.test.ts`
  - `apps/video-affiliate/worker/package.json`
- New endpoints:
  - `POST /api/dashboard/page-post-link-rewrite/check`
  - `POST /api/dashboard/page-post-link-rewrite/preview`
  - `POST /api/dashboard/page-post-link-rewrite/run`
- Checks already reported:
  - focused compile+run 84/84 pass
  - `npx tsc --noEmit --project tsconfig.json` clean
  - `git diff --check` clean
  - full `npm test` blocked by pre-existing dirty `test/ad-only-queue.test.ts` issue, unrelated.

Task now:
1. Inspect current diff briefly.
2. Deploy/restart the relevant `video-affiliate` Worker/API so the new endpoints are live.
3. Run read-only `check` for canary item.
4. Run read-only `preview` for canary item.
5. If and only if check+preview are clean, run exactly one real canary with `dry_run:false` for the item below.
6. Return concise evidence to aff-ads. Do not expose tokens/secrets.

Canary item:
- post_id/sub2: `768749526323172`
- old shortlink: `https://s.shopee.co.th/5q5l46qSw4`
- old expansion verified by aff-ads: `utm_content=1JUN26FBSPCAD-768749526323172-116759241338040--`
- resolved/expected page_id: `116759241338040` (do not use `1008898512617594` unless live resolve proves otherwise)
- target_sub1: `16JUN26FBSPCAD`
- customlink_id: `15130770000`
- surfaces: `["comment", "cta"]`
- allow_create_new: false

Required evidence back:
- deploy/restart command and success evidence
- check endpoint summary: page_id, page_story_object_id, comment_id, old comment link, old CTA link, old parsed subs
- preview summary: new_shortlink, target subs, sub4 policy/log id
- run result: ok/status, same comment_id, comment_verified, CTA_verified, old link gone, new link present, CTA final link, expanded new utm_content, affiliate id match
- if blocked, exact blocker/error code and no real write if preflight failed.

Hard safety:
- Do not batch other posts.
- Do not create duplicate comments.
- Do not delete anything.
- Stop on Graph/rate/spam blocks.
